import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    sidebarContainer: {
        flexDirection: 'row',
        gap: 5,
        width: '100%',
        paddingHorizontal: 15,
        alignItems: "center",
    },

    stepBackButton: {
        paddingHorizontal: 3,
    },
    sidebarTitle: {
        fontSize: 22,
        fontWeight: '700'
    }

})
