import { StyleSheet } from "react-native" 

export const styles = StyleSheet.create({
    container: {
        paddingTop: 47 ,
        flex: 1,
    },

    contentContainer: {
        display: 'flex',
        flexDirection: 'column',
    },

    sidebar: {
        marginBottom: 10,
        marginTop: 7
    }
})